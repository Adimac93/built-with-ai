# Pinned so `-D warnings` CI does not break on new stable clippy lints.
# Bump deliberately (rustup update stable + edit here + fix new lints).
[toolchain]
channel = "1.96.1"
components = ["clippy", "rustfmt"]
targets = ["wasm32-unknown-unknown"]
