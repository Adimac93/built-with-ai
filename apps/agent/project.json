{
  "name": "agent",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": ["agent", "python"],
  "targets": {
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "agents-cli deploy --project built-with-ai-gdg-wroclaw",
        "cwd": "apps/agent"
      }
    }
  }
}
